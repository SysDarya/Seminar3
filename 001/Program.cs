// Задать массив из 8 элементов и вывести их на экран

int[] array = { 1, 22, 3, 94, 5, 36, 7, 48};
int n = array.Length;
int index = 0;

while (index < n)
{
    Console.WriteLine(array[index]);
    index++;
}
